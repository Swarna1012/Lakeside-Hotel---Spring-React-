server:
  port: 9192

spring:
  datasource:
    username: postgres
    password: iphone21
    url: jdbc:postgresql://localhost:5432/lakeside
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
    servlet:
      multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2KB



